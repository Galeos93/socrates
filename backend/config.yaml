# Socrates Study Assistant Configuration

# OpenAI API settings
openai:
  api_key: ""  # Set via OPENAI_API_KEY environment variable
  completion_model: "gpt-4o"
  vision_model: "gpt-4o"

# Learning plan settings
learning_plan:
  max_knowledge_units: 3

# Study session settings
study_session:
  max_questions: 3
  max_knowledge_units: 3

# Server settings
server:
  host: "0.0.0.0"
  port: 8000
  reload: true  # Set to true for development

opik:
  project_name: "socrates"
  enable_tracking: true
